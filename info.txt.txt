git init
git add [file name] || -A
git status 
git commit -m "first commit"
git remote add origin reponame -[git@github.com:ganeshwakde/githublearning.git]
git push -u origin master

git clone origin master
git pull origin master
                 
				 
				 User connection
				 
				 
git config --global user.email "gwganesh88@gmail.com"
git config --global user.name "ganeshwakde"